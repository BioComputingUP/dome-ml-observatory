{
  "name": "dome-ml-observatory",
  "version": "0.1.0",
  "private": true,
  "description": "DOME Observatory monorepo: observatory-ui (Angular) + observatory-ws (NestJS). Each app manages its own independent install -- no shared '-core' package, no npm-workspaces hoisting.",
  "scripts": {
    "setup": "npm ci --prefix observatory-ui && npm ci --prefix observatory-ws",
    "lint": "npm run lint --prefix observatory-ui",
    "start": "npm run start --prefix observatory-ui",
    "build-dev": "npm run build-dev --prefix observatory-ui",
    "build-prod": "npm run build-prod --prefix observatory-ui",
    "deploy-prod-quick": "npm run deploy-prod-quick --prefix observatory-ui",
    "test": "npm run test --prefix observatory-ui",
    "start:ws": "npm run start:dev --prefix observatory-ws",
    "build:ws": "npm run build --prefix observatory-ws",
    "test:ws": "npm run test --prefix observatory-ws",
    "lint:ws": "npm run lint --prefix observatory-ws"
  }
}
